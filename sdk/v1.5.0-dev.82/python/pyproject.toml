[project]
name = "mediaviz-sdk"
version = "1.0.5.82.dev0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["mediaviz_sdk", "oauth_sdk"]
